# name: CI Pipeline

# on:
#   push:
#     branches:
#       - main

# jobs:
#   check-react-app:
#     runs-on: ubuntu-latest # Ensures compatibility with most applications

#     steps:
#       - name: Clone code
#         uses: actions/checkout@v3

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: "20"

#       - name: Install Dependencies
#         run: |
#           cd frontend
#           yarn install

#       - name: Start React App
#         run: |
#           cd frontend
#           yarn start &
#           sleep 2

#       - name: Check if React App is Running
#         run: |
#           curl -f http://localhost:3000/ || exit 1

#       - name: Stop React App
#         run: |
#           pkill -f "yarn start"  # Stop the running React app
