# Frontend / Client
CLIENT = https://trackmydegree.ca
FRONTEND_URL = https://trackmydegree.ca

# Database (adjust host if using a managed SQL instance or container name differs)
SQL_SERVER_USER = sa
SQL_SERVER_PASSWORD = MySecureP@ss123
SQL_SERVER_DATABASE = master
SQL_SERVER_HOST = sqlserver   # or actual IP / FQDN in production

DB_USER = sa
DB_PASSWORD = MySecureP@ss123
DB_NAME = master
DB_HOST = sqlserver

# Email (use an app password for Gmail if not already)
EMAIL_USER = trackmydegree@gmail.com
EMAIL_PASSWORD = tmns caut qynf ijay

# Environment
NODE_ENV = production

# JWT
JWT_SECRET = super-secret-secret    # ideally use a stronger random secret in prod
JWT_ACCESS_EXPIRY = 1h
JWT_REFRESH_EXPIRY = 7d
JWT_ORG_ID = trackmydegree.ca

# Session & Cookies
SESSION_ALGO = aes-256-gcm
COOKIE_DOMAIN = trackmydegree.ca

# Schedule (internal credentials)
SCHEDULE_USER = 866
SCHEDULE_PASS = cc94383af23d42ea7f5dcc9d0e2c4edc

# Backups
BACKUP_DIR = /var/backups
